<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<!--
	NOTE: The values which description include "[ENCRYPTED VALUE]" must be encrypted using the following
	script: /scripts/encrypt_cc_db_password.sh

	NOTE: startup=1 means that the property is only read/used at startup so the application must be restarted
		for changes to the property to take effect.
		startup=0 means that the property can be changed while the application is running.
-->
<default-data>
    <parameter>
        <name>test1</name>
        <value>value1</value>
        <description>test param</description>
        <environment>LIVE</environment>
        <startup>0</startup>
        <appname>er.account.service</appname>
    </parameter>
    <parameter>
        <name>erif.url</name>
        <value>http://localhost:8458/broker/router.jsp</value>
        <description>test param</description>
        <environment>LIVE</environment>
        <startup>0</startup>
        <appname>er.account.service</appname>
    </parameter>
</default-data>
